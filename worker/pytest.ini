[pytest]
testpaths = tests
# Worker modules import each other flat (`import parcel_gates`), the way
# pipeline.py runs them, so the package root goes on the path rather than
# the modules being rewritten for the tests' convenience.
pythonpath = .
filterwarnings =
    ignore::DeprecationWarning
